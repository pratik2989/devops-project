apiVersion: v1
kind: Service

metadata:
  name: devops-html-service
  namespace: devops-project

spec:
  selector:
    app: devops-html

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080

  type: NodePort
